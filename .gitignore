# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment secrets — NEVER commit these
server/.env
.env.local
server/.env.local

# Migration script backups — chứa dữ liệu học sinh thật, không commit
server/backups/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code — máy-cụ thể, không chia sẻ (skills/ vẫn được commit)
.claude/settings.json
.claude/settings.local.json
